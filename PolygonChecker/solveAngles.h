#pragma once

/*
* This function finds the angles of a triangle given its sides
* and determines if it is in fact a triangle and displays the angles
* 
* int: first side length
* int: second side length
* int: third side length
*/
void solveAngles(double*, double*, double*);